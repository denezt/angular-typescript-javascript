import { fromEvent, interval } from "rxjs";
import { debounceTime } from "rxjs/operators";
import { Event } from "jquery";

export class InputFeatures {    
    /**
     *
     */
    constructor(input: Event) {       
    }
}